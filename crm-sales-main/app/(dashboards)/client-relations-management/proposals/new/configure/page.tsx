import { ConfigureStep } from "@/components/client-relations-management/new-proposal/steps/configure-step";

export const metadata = {
  title: "New Proposal — Configure",
};

export default function ConfigureStepPage() {
  return <ConfigureStep />;
}
