import { BuilderStep } from "@/components/client-relations-management/new-proposal/steps/builder-step";

export const metadata = {
  title: "New Proposal — Builder",
};

export default function BuilderStepPage() {
  return <BuilderStep />;
}
